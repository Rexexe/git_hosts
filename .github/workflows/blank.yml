name: actions
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: chmod +x ./git_hosts_update.sh
      - run: chmod +x ./hosts
      - run: ./git_hosts_update.sh build
